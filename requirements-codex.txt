# Dev/test tooling (Codex should always have these)
ruff==0.6.9
pytest==8.3.3

# ccl_chromium_reader runtime (pin; update later if your Windows venv differs)
ccl_chromium_reader==0.3.14

# Dependencies used by the CCL ecosystem
Brotli==1.0.9
Jinja2==3.1.3
MarkupSafe==2.1.1
pycryptodome==3.19.1

# Windows-only dependency (safe to keep for documentation; skipped on Linux)
pywin32==306; platform_system=="Windows"

# Snappy helper
ccl_simplesnappy @ git+https://github.com/cclgroupltd/ccl_simplesnappy.git